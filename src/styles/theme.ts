import { Theme } from '@emotion/react';

const color = {
  primary: '#FF5C00',
};

const boxShadow = {
  button: '2px 2px 2px 1px rgba(0,0,0,0.25)',
};

const theme: Theme = {
  color,
  boxShadow,
};

export default theme;
